let http = require ("http");
let port = 8000;

function existeArchivo(solicitud, respuesta) {
  let origen = request.url

}


const fs = require(‘fs’)
fs.access(‘path’, fs.constants.F_OK, function (err) {
	if (err) {
		//El archivo no existe
  } else {
	//El archivo existe
  }
})
